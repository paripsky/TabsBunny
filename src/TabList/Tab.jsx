// import React from 'react';
// import PropTypes from 'prop-types';

// function Tab({ id, title }) {
//     return (
//         <div id={`tab-${id}`}>{title}</div>
//     );
// }

// Tab.propTypes = {
//     id: PropTypes.number.isRequired,
//     title: PropTypes.string.isRequired,
// };

// export default Tab;
